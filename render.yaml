services:
  - type: web
    name: sahaay-guidewire-2026
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install; npm --prefix server install; npm --prefix client install; npm --prefix client run build
    startCommand: npm --prefix server start
    healthCheckPath: /healthz
    autoDeploy: true
